the way I went about the assignment was that I first created the repo in github then i created the first branch in and called it feature/header by writing git checkout -b feature/header, then i did the same step for other branches, one called feature/footer and another called review/main. on the feature/header branch I added a header and on the feature/footer branch I added a footer. then I merged the two branches into the main branch. I added a small change into the review/main branch by adding a p tag then I pushed the branch into my repository, then did a pull request on github and did a merge so that the p tag then would show on the main branch in git hub and finally i added 